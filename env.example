# Dashboard Configuration
DASH_USER=jacob
DASH_PASS=jacob
DASH_PORT=3000

# MediaMTX Ingest Authentication
# Single static bearer token for LiveU camera ingest
# Generate with: openssl rand -hex 32
MTX_INGEST_KEY=your-secure-random-key-here-64-characters

# MediaMTX Ingest Host (domain/IP only - ports are standard)
# Clients will connect to this hostname on ports 1935 (RTMP) and 8890 (SRT)
# For local testing: 127.0.0.1
# For production: your-domain.com or your-ip-address
MTX_INGEST_HOST=127.0.0.1

# Ingest Protocols
# RTMP (port 1935):
#   Format: rtmp://host:port/path?user=USERNAME&pass=PASSWORD
#   Example: rtmp://rsr-ingest.me3za.me:1935/liveu?user=liveu&pass=KEY
#   Use for: Stable networks, fixed locations
#
# SRT (port 8890):
#   Format: srt://host:port?streamid=publish:path:user:pass
#   Example: srt://rsr-ingest.me3za.me:8890?streamid=publish:liveu:liveu:KEY
#   Use for: Unreliable networks, mobile cameras (LiveU preferred)
#   Benefits: Encryption, reliability, low latency

# External Forwarding to Cloud OBS (optional)
FORWARD_ENABLED=false
RTMP_URL=
STREAM_KEY=

# DigitalOcean Spaces Configuration (S3-compatible storage)
SPACES_ACCESS_KEY=
SPACES_SECRET_KEY=
SPACES_REGION=nyc3
SPACES_BUCKET=
SPACES_ENDPOINT=

# Email Notifications
SENDGRID_API_KEY=
RECIPIENT_EMAIL=

